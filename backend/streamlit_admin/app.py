"""
Streamlit admin / developer testing UI.

Lets a developer run any agent directly against the FastAPI backend
and inspect raw input/output.  This is NOT the founder-facing product —
it is a debug/demo tool only.

Will be filled in a later prompt.
"""

import streamlit as st

st.set_page_config(page_title="Cofounder AI — Admin", layout="wide")
st.title("🛠️ Cofounder AI — Admin Panel")
st.info("Placeholder — agent testing UI will be built in a later prompt.")
